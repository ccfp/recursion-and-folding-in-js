// What are x and y?
const [x, y] = [0, 1, 2, 3, 4, 5]

// Sample array
const abc = ["a", "b", "c"]

// Define a head and tail function
const head = () => undefined
// head(abc) //=> "a"
head(abc) //?

const tail = () => undefined
// tail(abc) //=> ["b", "c"]
tail(abc) //?

module.exports = { head, tail }
